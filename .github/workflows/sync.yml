name: Sync from IHEP GitLab

on:
  schedule:
    # 08:00 UTC
    - cron: '0 8 * * *'
    # 16:00 UTC
    - cron: '0 16 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Configure git
        run: |
          git config --global user.name "heris-sync-bot"
          git config --global user.email "heris-sync-bot@example.com"

      - name: Mirror from IHEP GitLab to GitHub
        env:
          GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 1. Clone mirror repo from IHEP GitLab
          git clone --mirror "https://oauth2:${GITLAB_TOKEN}@code.ihep.ac.cn/albert_chg9/heris.git" repo.git

          cd repo.git

          # 2. Add GitHub as push target
          git remote add github "https://x-access-token:${GITHUB_TOKEN}@github.com/albertc9/HERIS.git"

          # 3. Push all refs (branches, tags) as a complete mirror to GitHub
          git push --mirror github
